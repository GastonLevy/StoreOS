name: Deploy StoreOS to VPS

on:
  push:
    branches:
      - release/vps-ready

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test workflow
        run: echo "Workflow detected correctly"
